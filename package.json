{
  "name": "calendrier",
  "version": "1.0.0",
  "description": "page qui affiche le calendrier mois par mois",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc && xcopy src\\html dist\\html /s /y && xcopy src\\styles dist\\styles /s /y",
    "start": "npm run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^20.0.0"
  },
  "dependencies": {
    "@syncfusion/ej2-schedule": "^20.2.40",
    "mysql2": "^2.3.3"
  }
}
